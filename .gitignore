# deps / builds
node_modules
dist
.ssr
.vercel

# secretos
.env*

# material de trabajo pesado (no va al repo)
deliverables
glb-originals-backup
imagenes14prodesarmado

# SO / editor
.DS_Store
Thumbs.db

# fotos personales de referencia (no van al repo público)
fotos-referencia
